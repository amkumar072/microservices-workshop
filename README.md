Client (React):

1) Enter into the folder Client.
2) Install dependencies -> npm install 
3) Run -> npm start

Server (Node/Express):

1) Enter into the folder Server.
2) Install dependencies -> npm install 
3) Run -> npm start